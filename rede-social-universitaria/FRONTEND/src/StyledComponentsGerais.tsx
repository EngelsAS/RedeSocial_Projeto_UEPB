import { Col, Row } from "react-bootstrap";
import styled from "styled-components";

export const RowInterna = styled(Row)`
  margin: 0;
  padding: 0;
`;

export const ColInterna = styled(Col)`
  padding: 0;
`;
